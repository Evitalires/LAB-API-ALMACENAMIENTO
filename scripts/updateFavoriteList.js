export const updateFavorite = (pokemon)=>{
console.log("estoy en favorito", pokemon)
// traer con parse lo que tengo en el localstorge
 // filtro .find y busco el pokemons - nombre o id
 // cuando lo encuentre lo remueve del array y vuelve a el array completo al local estorage

 //actualiza lo que se esta mostrando
}